# 09 - YOLOv8 TensorRT Python

Goal: build a Python TensorRT reference pipeline for faster debugging.

Topics:

- TensorRT Python runtime
- NumPy preprocessing
- YOLO output decode
- NMS
- Visualization

Acceptance criteria:

- The script runs inference on a test image and saves a detection result.
- Output is checked against the PyTorch or Ultralytics reference.
